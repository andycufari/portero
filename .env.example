# Server
PORT=3055
BEARER_TOKEN=replace-me

# Telegram
TELEGRAM_BOT_TOKEN=
# Optional: if not set, pair at runtime via Telegram /pair <PAIRING_CODE>
TELEGRAM_ADMIN_CHAT_ID=

# Pairing
PAIRING_CODE=

# Approval settings
APPROVAL_TIMEOUT_SECONDS=300

# Anonymization data (referenced in replacements.json)
REAL_NAME=
REAL_EMAIL=
REAL_API_KEY=

# MCP environment (referenced in mcps.json)
GITHUB_TOKEN=

# Google Workspace MCP (referenced in mcps.json)
# Setup: https://console.cloud.google.com → APIs & Services → Credentials → OAuth 2.0 Client ID (Desktop app)
# Enable APIs: Gmail API, Google Calendar API, Google Drive API
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
